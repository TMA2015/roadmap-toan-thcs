# CĐ18 — góc nâng/góc hạ SVG replacement

- Scope: two new original SVGs: `18-goc-ha-v1.svg` and `18-goc-nang-goc-ha-v1.svg`.
- The latter replaces the old Adobe Illustrator image **at its existing lesson location**; the old file remains in the repository for rollback and provenance.
- Both angles are at observation point O and measured relative to the same horizontal ray, not between the two sight lines.
- The individual depression diagram has P below O, a horizontal reference and no implied equality of lengths.
- SVG uses internal style, Vietnamese labels, transparent background, title/desc/alt and standard viewBox.
- An existing CI pilot validator now checks the two new paths, basic semantics and no legacy reference in the lesson. This is not a substitute for real device visual inspection.
- Other four pilot SVGs and infographic files unchanged.
